{{- if .Values.platformTesting.enabled -}}
apiVersion: v1
kind: Service
metadata:
  name: {{ include "pnda.platform-testing.fullname" . }}-kafka-jmx
  labels:
    {{- include "pnda.platform-testing.labels" . | nindent 4 }}
spec:
  ports:
    - port: 5555
      name: jmx
  selector:
    app.kubernetes.io/instance: {{ .Release.Name }}
    app.kubernetes.io/name: cp-kafka
{{- end -}}